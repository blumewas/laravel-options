- Create Base Option class
    - support crud methods (/)
    - support casts for values (/)

- service support retrieve for models/group
    - cache?

- Allow inject into config (/)
- Create Trait for Options on Models
- Group from enum
